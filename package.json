{
  "name": "shift",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run generate:changelog && nuxt build",
    "dev": "nuxt dev",
    "generate": "npm run generate:changelog && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate:changelog": "node scripts/generate-changelog.mjs"
  },
  "dependencies": {
    "@nuxt/ui": "^4.4.0",
    "date-fns": "^4.1.0",
    "mysql2": "^3.16.3",
    "nuxt": "^4.3.0",
    "vue": "^3.5.27",
    "vue-router": "^4.6.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^25.2.3"
  }
}